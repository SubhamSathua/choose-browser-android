<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background/Track -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="12dp" />
            <solid android:color="@color/previewPage_secondary" />
        </shape>
    </item>
    <!-- Progress (filled) -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="12dp" />
                <solid android:color="@color/previewPage_btnPrimary" />
            </shape>
        </clip>
    </item>
</layer-list>
